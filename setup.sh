./Setup/build_libs.sh
./Setup/build_app.sh
echo "WARNING!"
echo "For new components compiling, you MUST set the next environnement variable."
echo "QT_ROOT_DIR (Path to the root QT install directory)"
echo "QT_INCLUDEDIR (Path to the directory where QT headers are located)"
echo "QT_LIBRARYDIR (Path to the directory where QT libraries are located)"
